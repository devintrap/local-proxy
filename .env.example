# Project name - used as a prefix for containers
COMPOSE_PROJECT_NAME=local-proxy

# Network configuration - default values can be kept
SUBNET=10.0.0.0/16 # Internal network subnet
GATEWAY=10.0.0.1   # Network gateway

# Domain configuration - REQUIRED
DOMAIN_NAME=example.com # Your domain name configured in Cloudflare

# SSL configuration - REQUIRED
SSL_EMAIL=your-email@example.com # Email for Let's Encrypt notifications

# Cloudflare configuration - REQUIRED
CLOUDFLARE_API_EMAIL=your-cloudflare@email.com     # Your Cloudflare account email
CLOUDFLARE_DNS_API_TOKEN=your-cloudflare-api-token # Cloudflare API token with DNS edit permissions

# Traefik version - can be changed to specific version if needed
TRAEFIK_VERSION=3.2.0
TRAEFIK_IP=10.0.0.2 # Static IP for Traefik proxy
